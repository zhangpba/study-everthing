此包下面对应了牛客网上103道机试题的答案：
https://www.nowcoder.com/exam/oj/ta?page=1&tpId=37&type=37

public class Test07 {

    public static void main(String[] args) {
        System.out.println("A-a= " + ('A' - 'a'));

        String I = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
        String i = "abcdefghijklmnopqrstuvwxyz";
        for (int j = 0; j < I.length(); j++) {
            if ('A' <= I.charAt(j) && I.charAt(j) <= 'a') {
                System.out.println("26个大小写字母为: " + I.charAt(j) + i.charAt(j) + "两个相减：" + (i.charAt(j) - I.charAt(j)));
            }
        }
    }
}

1、获取集合中的最小值：
    Collection<Integer> values = map.values();
    Integer min = Collections.min(values);

2、String
    substring(0,1)：截取第一个字符
    substring(1)：截取第一个字符后边的所有字符
    charAt(i)：获取字符串中i位置的char字符

3 先将num转化为2、8、16进制
    Integer中有一些将10进制转化为 2（toBinaryString）、8（toOctalString）、16（toHexString）进制的方法
    String str = Integer.toBinaryString(num);
    String str = Integer.toOctalString(num);
    String str = Integer.toHexString(num);